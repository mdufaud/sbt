# sbt: enable Gradle build cache and configuration cache. The build cache is
# pointed at the build tree (see the buildCache block injected into
# settings.gradle) so `make clean` wipes it along with the APK outputs.
org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.configuration-cache.problems=warn
org.gradle.parallel=true
